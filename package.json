{
  "name": "folio",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "portless proxy start --port 8080 --no-tls; turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "portless": "^0.15.1",
    "turbo": "^2"
  },
  "packageManager": "bun@1.3.14"
}
